apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apyrobo-audit-pvc
  namespace: apyrobo
  labels:
    app.kubernetes.io/name: apyrobo
    app.kubernetes.io/component: api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
